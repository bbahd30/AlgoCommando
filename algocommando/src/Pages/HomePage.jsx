import React from 'react';

const HomePage = () => {
    return (
        <div>
            HomePage
            {/* to show the categories we are going to show */}
        </div>
    );
};

export default HomePage;